name: Static/Runtime Analysis

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  lint-code:
    runs-on: windows-latest
    steps:
      - name: Checkout your Git repo
        uses: actions/checkout@v2

      - name: Install aftman and all aftman tools (rojo and mantle)
        uses: ok-nick/setup-aftman@v0.4.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Wally Modules
        run: scripts/wally-install.cmd

      - name: Lint unit tests
        run: luau-lsp analyze --sourcemap sourcemap.json --base-luaurc .luaurc --defs globals/testez.d.luau tests
      - name: Lint story books
        run: luau-lsp analyze --sourcemap sourcemap.json --base-luaurc .luaurc stories
      - name: Lint source code
        run: luau-lsp analyze --sourcemap sourcemap.json --base-luaurc .luaurc src

  check-format:
    runs-on: windows-latest
    steps:
      - name: Checkout your Git repo
        uses: actions/checkout@v2

      - name: Check Format [Source code]
        uses: JohnnyMorganz/stylua-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: 0.20.0
          args: --config-path stylua.toml --check src

      - name: Check Format [Story books]
        uses: JohnnyMorganz/stylua-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: 0.20.0
          args: --config-path stylua.toml --check stories

      - name: Check Format [Unit tests]
        uses: JohnnyMorganz/stylua-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: 0.20.0
          args: --config-path stylua.toml --check tests

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your Git repo
        uses: actions/checkout@v2

      - name: Run Tests
        run: ./scripts/shell/test.sh default.project.json $TEST_TASK_FILE
        env:
          ROBLOX_API_KEY: ${{ secrets.TEST_RUNNER_OPEN_CLOUD_API_KEY }}
          ROBLOX_UNIVERSE_ID: ${{ vars.TEST_RUNNER_UNIVERSE_ID }}
          ROBLOX_PLACE_ID: ${{ vars.TEST_RUNNER_PLACE_ID }}
          TEST_TASK_FILE: tests/run.luau
